# Path of Exile 2 Log Parser

This app parses the Path of Exile 2 client log and extracts entries related to endgame locations, then displays the results.

## How to use

Place the executable inside the game's `logs` folder, located next to `Client.txt`, and run the application.

## Requirements

- .NET 10
